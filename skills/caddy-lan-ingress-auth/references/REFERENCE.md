# Caddy — reference

Legacy: `scripts/linux-lan/Caddyfile`, `Caddyfile.cidr`, `Caddyfile.tls-internal`, ansible role `caddy_gateway`.

Vars: `caddy_variant`, `caddy_basic_auth_user`, `caddy_basic_auth_hash`, `lan_cidr_allowlist` in `infra/ansible/group_vars/bench.yml`.
