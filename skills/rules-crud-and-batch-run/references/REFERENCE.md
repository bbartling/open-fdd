# Rules service — reference

Legacy: `open_fdd/desktop/rules/`, gateway `/rules/*`, `/rules/run`.

Default install: `GET /rules/defaults`, `POST /rules/defaults/install`.

Cookbook: `docs/expression_rule_cookbook.md`.
