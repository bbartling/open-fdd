---
name: openclaw-integrations
description: "Wires optional OpenClaw cron and chat helpers through the bridge and dashboard advanced panel. Use when operators already run OpenClaw alongside Open-FDD."
---

# OpenClaw integrations

Optional env in dashboard `.env.example`; gateway `openclaw_*` and `openfdd_claw_*` modules; MCP OpenClaw-oriented tools when action tools enabled.

Compose with [codex-agent-on-bridge](../codex-agent-on-bridge/SKILL.md).

Legacy UI libs: `openclaw-ops.ts`, `openfdd-claw-*.ts`.
