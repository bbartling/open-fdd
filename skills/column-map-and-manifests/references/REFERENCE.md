# Column map — reference

Legacy desktop model sync used `model.json` + TTL under `OFDD_DESKTOP_DATA_DIR` (see feather-local-storage skill). Engine tests in `open_fdd/tests/engine/test_column_map_from_model.py` document expected manifest shapes.

Resolver implementations: `open_fdd/engine/column_map_resolver.py`.

Docs: `docs/column_map_resolvers.md`.
