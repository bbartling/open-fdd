# Cursor prompt — finish Wave U acceptance

Continue the existing Open-FDD Wave U work. Read these first:

1. `AGENTS.md` and `openfdd_agent_spec/AGENTS.md`.
2. `.cursor/plans/wave_u_independent_acceptance_audit.plan.md` in full.
3. `MILESTONES.md`, `docs/operations/WAVE_U_MASTER.md` and `docs/operations/BUG_REPORT_WAVE_P.md`.
4. The active Wave U FQ plan and the existing S5/S3/S4 child requirements linked by the audit.

Execute the corrective work, not just another plan. Recheck current HEAD/PRs and preserve in-flight changes. Address UA-01 through UA-10, including the 14 reproduced evaluator failures, with permanent negative and healthy controls at the real product/evaluator/wrapper boundaries. Fix required gate registration and evidence validation before relying on a green stress summary. Both gate 36 checks must block the promised closeout when they fail.

Finish secure standalone and field-only deployment readiness now. Lack of a Nessus license blocks only the actual licensed assessment. Complete real candidate TLS/exposure, host/runtime, image remediation, generated MQTT identity/ACL/key tests and authenticated isolated ZAP coverage. Do not qualify from skipped broker tests, stub/reference-image scans, unresolved required findings or a PR merge. Keep physical OT writes and active scanning out of the live hub workflow.

Complete the inherited modeling/ECM, wheel and SQL twin obligations using their existing plans. After affected candidate tests and CI pass, follow the authorized backup/publish/re-pin workflow and run the final enhanced Railway MEGA with the repaired required gate contract and pre/post checks. Record each failed attempt before its fix, retain immutable evidence, then document the exact candidate and retest results in BUG_REPORT and MILESTONES.

Continue all unblocked work. If a genuine external dependency remains, name the owner, missing evidence and next action without cancelling the requirement or calling it verified. Finish with a concise milestone-by-milestone account of implementation, measured verification, release status and any remaining blockers. Keep private findings/credentials out of public issues and Pages; prove the documented workflow from a clean developer environment.
