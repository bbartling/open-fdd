---
# Insurance checks after Open-FDD edge deploy (import from deploy.yml or run standalone).
# Standalone: ansible-playbook -i inventory.yml post_deploy_check.yml --limit bacnet_pi

- name: Post-deploy insurance checks
  hosts: pi_bcn
  gather_facts: false

  tasks:
    - name: Run post-deploy insurance checks
      ansible.builtin.include_tasks: tasks/post_deploy_check.yml
