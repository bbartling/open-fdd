# Windows batch files must use CRLF so cmd.exe parses line endings correctly.
*.cmd text eol=crlf

# Shell scripts must use LF (bash in Docker/Linux breaks on CRLF from Windows checkouts / docker cp).
*.sh text eol=lf
