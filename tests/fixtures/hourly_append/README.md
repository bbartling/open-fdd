# Hourly append CI fixture

Tiny 15-minute SAT series used by `fdd_store` merge tests. Real IoT appenders
POST `confirm:true` + JWT to `/api/csv/import/package/append`. Do not commit
Building 50 vendor zips here.
