---
name: 3.3.33 mqtt overview csv parity
overview: "Tiny VERSION 3.3.28→3.3.33. MQTTS live sites populate Overview / AFDD / Plots the same as CSV packages (historian building_id= scope for FDD inventory+run+series + building picker). Full Railway stress LAST. Low-RAM."
todos:
  - id: p0-hygiene
    content: GH hygiene START — 0 open PRs; only master tip Actions green
    status: in_progress
  - id: p1-version
    content: Bump VERSION + Cargo 3.3.28 → 3.3.33
    status: pending
  - id: p2-fix
    content: FDD equipment/run/series use register_historian_building; inventory walks history/building_id=; buildings list unions MQTT historian sites
    status: pending
  - id: p3-pr
    content: One PR; squash-merge; wait GHCR Publish (mqtt+fieldbus)
    status: pending
  - id: p4-repin
    content: Railway backup + re-pin central→mqtt→web; x86 fieldbus same sha-*
    status: pending
  - id: p5-stress
    content: run_railway_hub_stress.sh + Overview UI probe (bldg2 equipment non-empty, Zone Other tables, fdd/run)
    status: pending
  - id: p6-bug-report
    content: BUG_REPORT verdict 3.3.33 CLOSED; track soft leftovers
    status: pending
  - id: p7-hygiene-end
    content: GH hygiene END
    status: pending
isProject: false
---

# 3.3.33 — MQTTS Overview = CSV Overview

**Parent:** nightly 3.3.27+ program CLOSED. New patch cycle.

## Concern (one)

SPA Overview stays on empty CSV hero for live MQTT `bldg2` because `/api/fdd/equipment` (and run/series) only walk legacy `building={id}/`, while analytics already use canonical `history/building_id={id}/` (#851).

## Prove

- `GET /api/fdd/equipment?building_id=bldg2` non-empty (includes `bldg2-zone-loopback`)
- Overview `?site=bldg2` shows populated chrome + Zone Other tables (not collapsed empty)
- `POST /api/fdd/run` with `building_id=bldg2` succeeds / writes scoped results
- CSV `BUILDING_100` regression still lists equipment
- Full `run_railway_hub_stress.sh` → `fully_qualified=true`

## Out of scope

- Restoring Plotly onto Overview (RCx keeps charts)
- Soft-OPEN `railway-ui-fdd-stale` deeper FDD chrome (partially helped by picker union)
