# Open-FDD 3.2.5 (development patch cycle)

**Branch:** `release/rust-3.2.5-patch-cycle`  
**Base release:** 3.2.4 on GHCR (`ghcr.io/bbartling/openfdd-edge-rust`)

## Focus for field testing

- TADCO / sidecar CSV agent ingest smoke via MCP playbook
- Preflight → execute → FDD batch → report PDF loop
- Bench validation with `OPENFDD_DRIVERS_VALIDATE_STRICT=1`

## Local validation

```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cd workspace/dashboard && npm ci && npm run build && npm test -- --run
```
