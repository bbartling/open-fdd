# Open-FDD 3.2.8

**Bench input:** [#429](https://github.com/bbartling/open-fdd/issues/429) closeout report (3.2.7 NO sign-off)  
**Goal:** Fix P0 poll→historian and BACnet field-tree gaps; ship deferred 3.2.8 scope.

## P0 fixes (bench FAIL → PASS target)

| FIX | Change |
|-----|--------|
| FIX-2 | `POST /api/modbus/poll-once` + `/api/modbus/poll/once`; poll cycles/errors in status; 60s default interval; commission poll loop |
| FIX-8 | Who-Is merge excludes local server 599999; merge missing field devices; full discover range when env unset |
| FIX-14 | `POST /api/modbus/refresh` alias → read handler (bench scripts + legacy clients) |
| FIX-15/16 | BACnet Who-Is accepts `range_low`/`range_high`; UI sends `low_limit`/`high_limit`; poll bootstrap no longer seeds local instance as field device |
| FIX-36 | `GET /api/agent/validate` includes modbus poll snapshot + historian row_count |

## Also in 3.2.8

- **#430** README semver + `:latest` guidance (pinned tags on benches)
- **#431** Agent validate structured driver snapshot (partial — host bootstrap still script-only)
- **#436** SPARQL predefined `fdd_zn_t_points` catalog entry
- Profile: `OPENFDD_BACNET_DEVICE_INSTANCE` no longer overwrites validation `device_instance` (local server stays separate)

## Deferred to 3.2.9 / open issues

- **#432** Liberty infer-plan backend + execute E2E
- **#433** Full driver UX spinners + CSV delete checkbox UI
- **#434** Full Selenium/Vitest matrix
- **#435** ZAP re-run artifact triage
- **#437** oxigraph / quick-xml bump (audit ignore retained)

## Bench re-run after publish

```bash
cd /home/ben/open-fdd
OPENFDD_IMAGE_TAG=3.2.8 ./scripts/openfdd_src_sync_for_test.sh
NEW_TAG=3.2.8 OPENFDD_RESTORE_HISTORIAN_AFTER_UPDATE=1 ./scripts/openfdd_rust_site_update.sh
openfdd_rust_dcompose up -d --force-recreate
OPENFDD_REV326_POLL_CYCLES=5 ./scripts/openfdd_rev326_rigorous_report.sh
```
