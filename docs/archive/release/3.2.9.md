# Open-FDD 3.2.9

**Bench input:** [#429](https://github.com/bbartling/open-fdd/issues/429) 3.2.8 closeout + Python parity analysis (FIX-43–47)

## P0 — Feather store + poll parity (Python → Rust)

| FIX | Change |
|-----|--------|
| FIX-43 | New `edge/src/historian/feather_store.rs` — shard write, list_sites, total_bytes |
| FIX-44 | Modbus poll dual-write: feather shard + pivot row |
| FIX-45 | BACnet poll dual-write feather + pivot on successful reads |
| FIX-46 | Who-Is cache (`whois_discovered.json`) + merge on tree/whois; field-device sync loop |
| FIX-47 | `openfdd_post_update_data_recovery.sh` — chown historian/feather to UID 10001; auth 644 |

## CI / build

- `Swatinem/rust-cache` on release test job
- npm cache for dashboard in release workflow
- Docker `CARGO_BUILD_JOBS=4` (was 2)

## Bench re-run

```bash
cd /home/ben/open-fdd
OPENFDD_IMAGE_TAG=3.2.9 ./scripts/openfdd_src_sync_for_test.sh
NEW_TAG=3.2.9 OPENFDD_RESTORE_HISTORIAN_AFTER_UPDATE=1 ./scripts/openfdd_rust_site_update.sh
openfdd_rust_dcompose up -d --force-recreate
find workspace/data -name '*.feather'
OPENFDD_REV326_TAG=3.2.9 OPENFDD_REV326_POLL_CYCLES=5 ./scripts/openfdd_rev326_rigorous_report.sh
```

## Still deferred

- Full feather compact/prune/maintenance (Python parity P1)
- BACnet CSV long→wide ingest worker (commission samples.csv watcher)
- ZAP/Caddy matrix (#435), full Selenium matrix (#434), oxigraph (#437)
