# Open-FDD 3.2.6 (development patch cycle)

**Branch:** `release/rust-3.2.6-patch-cycle`

## Highlights

- Dashboard fault analytics: 2-decimal rounding, multi-sensor per-column stats
- SQL FDD tab: table picker + SQL editor + Format SQL (removed NL/visual builder)
- Default Haystack model: `site:local` + `equip:local-test-equipment` when no grid on disk
- Carries 3.2.5 GHCR semver tags, poll→historian fixes, login dev gating

## Bench pin

```toml
# workspace/bench/bench_profile.toml
edge = "ghcr.io/bbartling/openfdd-edge-rust:3.2.6"
mcp = "ghcr.io/bbartling/openfdd-mcp:3.2.6"
```
