# Open-FDD 3.2.7 (development patch cycle)

**Branch:** `release/rust-3.2.7-patch-cycle`  
**Bench input:** REV_326 rigorous report @ `d66942a3` / GHCR `3.2.6`  
**Goal:** Close P0 bench FIX items; tag `3.2.7`; GHCR + GitHub Pages green.

## PR merge order (optimized)

| # | PR / theme | FIX IDs | Depends on |
|---|------------|---------|------------|
| 1 | Docs Gemfile + prune bash | — | — (merged #427, #428) |
| 2 | Driver quick wins + CSP | 14, 15, 19, 21 | — |
| 3 | Poll → historian + default equip | 2, 3 | — |
| 4 | CSV Fusion UI → backend | 37, 38, 40 | — |
| 5 | Agent `/agent` onboarding | 24, 25, 26, 28 | — |
| 6 | Live FDD demo gate | 23 | — |
| 7 | BACnet tree + overrides | 8, 17, 18 | 2 |
| 8 | Haystack live read | 16 | — |
| 9 | MCP Liberty recipe + tests | 39, 42 | 4 |
| 10 | Agent bootstrap stubs → real | 29, 30, 36 | 5 |
| 11 | Release 3.2.7 + bench re-run | — | 1–9 |

## Deferred batch (same cycle — commit 2)

| FIX | Status | Notes |
|-----|--------|-------|
| 8, 17–18 | Done | Who-Is merge into empty BACnet field tree; override scan device rotation |
| 1 | Done | `bacnet_live` client `stop_client()` on all I/O paths (FD leak) |
| 16 | Done | Haystack `driver_tree_json` live read refresh when configured |
| 29–30, 36 | Done | `POST /api/agent/bootstrap|update` script pointers; `GET /api/agent/validate` |
| 40 | Done | CSV execute `delete_staged_files: true` |
| 39, 42 | Done | MCP Liberty long-format recipe; fixture + delete-staged tests |

**Architecture note:** `bacnet_live.rs` is the rusty-bacnet wire-I/O adapter (not a simulated-data path). The facade is `bacnet.rs`. Non-live `OPENFDD_BACNET_MODE` returns API errors; it does not synthesize OT values. Default equipment id renamed to `equip:local-default` (audit-safe).

## P0 acceptance (bench sign-off)

- [ ] `samples > 0` after poll window; historian row growth
- [ ] Modbus/BACnet/Haystack driver refresh actions work in UI
- [ ] CSV drop → `/api/csv/import/preview` → session list
- [ ] `/agent` copy MCP + briefing; pinned image tag
- [ ] PDF preview renders (CSP `frame-src blob:`)
- [ ] `prune-ghcr` job green on Rust Release
- [ ] Liberty `hvac_systems_CLEANED` preview/plan via UI or MCP

## Deploy

```bash
NEW_TAG=3.2.7 OPENFDD_RESTORE_HISTORIAN_AFTER_UPDATE=1 ./scripts/openfdd_rust_site_update.sh
OPENFDD_REV326_POLL_CYCLES=5 ./scripts/openfdd_rev326_rigorous_report.sh
```
