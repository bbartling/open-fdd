# Minimal rule for test_runner_from_dir: uses column "sat" from sample_df (no column_map)
name: test_flatline_sat
description: Flatline on SAT for engine tests
type: flatline
flag: flatline_flag

inputs:
  sat:
    column: sat

params:
  tolerance: 0.000001
  window: 5
