# Examples

Small sanitized CSV or package snippets for local experiments only.

Do **not** commit client historian exports. Prefer:

- An `openfdd_package_v1` zip loaded in Streamlit or via `scripts/agent_afdd.py`
- Or `HVAC_DATA_ROOT` / folder path on your machine (native local mode)

See the app [`README.md`](../README.md) and [`docs/PACKAGE_SPEC.md`](../docs/PACKAGE_SPEC.md).
