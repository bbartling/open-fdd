# WattLab / agent AFDD exporter (no React SPA — used by openfdd-central job dumps)
pandas>=2.0
pyyaml>=6.0
duckdb>=0.9
pydantic>=2.0
requests>=2.31
# Pandas oracle rules/analytics from PyPI (not a production FDD runtime)
open-fdd[oracle]>=4.1.0,<5
