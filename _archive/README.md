# Archived reference material

| Path | Status |
|------|--------|
| `examples_pandas_yaml/` | Retired pandas/YAML notebook examples — not used by edge bridge or CI |

`open_fdd.engine` remains in the repo for the optional `openfdd-engine` wheel and historical tests; new FDD work is Arrow-native via `apply_faults_arrow`.
