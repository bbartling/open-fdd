# Long-term memory (Open-FDD workspace)

Use this file for **durable facts** the team agrees should persist across sessions: site nicknames, chosen `rules_path`, BACnet device quirks, CSV encoding notes, last good `POST /plots/share` id, etc.

**Convention**

- Newest notes at the **bottom** under dated headings.
- Never store **secrets** here (tokens, passwords) — use OpenClaw auth profiles and bridge env vars.

## Template entries

### YYYY-MM-DD

- _Example:_ Site `…` uses joined sources; CHW DP column is `Chilled_Water_Differential_Pressure_Sensor_csv`.
