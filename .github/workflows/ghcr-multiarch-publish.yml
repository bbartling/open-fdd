# DISABLED — legacy Python multi-arch GHCR helper.
# Current deployments use .github/workflows/rust-ghcr.yml → ghcr.io/bbartling/openfdd-edge-rust

name: Publish multi-arch GHCR images (legacy Python — disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy Python multi-arch publish removed
        run: |
          echo "Retired Python stack publishing. Use rust-ghcr.yml for the Rust edge image."
          exit 1
