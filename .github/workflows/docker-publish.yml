# DISABLED — legacy Python stack GHCR images (openfdd-bridge, openfdd-commission, …).
# Current deployments use .github/workflows/rust-ghcr.yml → ghcr.io/bbartling/openfdd-edge-rust

name: Publish Docker images to GHCR (legacy Python — disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy Python GHCR publish removed
        run: |
          echo "This workflow published retired Python stack images."
          echo "Use .github/workflows/rust-ghcr.yml for ghcr.io/bbartling/openfdd-edge-rust"
          exit 1
