apiVersion: 1
datasources:
  - name: TimescaleDB
    type: postgres
    uid: openfdd_timescale
    access: proxy
    url: db:5432
    database: openfdd
    user: postgres
    secureJsonData:
      password: postgres
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
