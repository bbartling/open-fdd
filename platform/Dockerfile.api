# Open-FDD CRUD API — sites, points, equipment
FROM python:3.12-slim
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY pyproject.toml README.md MANIFEST.in ./
COPY open_fdd ./open_fdd
RUN pip install --no-cache-dir -e ".[platform,brick]"

EXPOSE 8000
CMD ["uvicorn", "open_fdd.platform.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
