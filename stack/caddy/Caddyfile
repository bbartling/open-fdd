# Open-FDD reverse proxy (dev: no auth; add basic_auth for production — see docs/security.md).
# Port 80 → frontend only. Use host:8000 for API/Swagger; other services by their ports.

:80 {
	handle /* {
		reverse_proxy frontend:5173
	}
}
