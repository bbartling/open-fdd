# Workspace memory

Durable operator context for Codex wakes and portfolio agents.

| File | Purpose |
|------|---------|
| `../MEMORY.md` | Live bootstrap (gitignored) |
| `../MEMORY.md.example` | Template — no secrets |
| `sites.example.md` | Per-site notes template |
| `rule-tuning-notes.example.md` | FDD tuning history template |

Promote stable facts from daily notes into `MEMORY.md`; keep credentials in `workspace/auth.env.local` only.
