# Open-FDD operator memory (example — copy to workspace/MEMORY.md)

No secrets in this file. Live `workspace/MEMORY.md` is gitignored.

## Sites

- **acme** — GL36 lab, Tailscale edge, integrator auth via env (not stored here)

## Stable decisions

- Acme edge: MCP disabled; portfolio agent calls bridge over Tailscale
- FDD tuning: dry-run `apply-tuning` before any `apply=true`

## Links

- Patch cycles: `skills/openfdd-edge-deploy-tune/SKILL.md`
- MCP: `docs/ai/mcp-server.md`
