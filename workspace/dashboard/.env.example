# Central API origin for the dashboard (Vite build-time).
# Leave empty for same-origin (reverse proxy serves UI + /api on one host).
VITE_API_BASE=

# Legacy desktop bridge override (prefer VITE_API_BASE for new deployments).
# VITE_DESKTOP_BRIDGE_BASE=http://127.0.0.1:8080
