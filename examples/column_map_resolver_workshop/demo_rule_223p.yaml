# 223P-scoped labels as valid Python identifiers — pair with manifest_223p_safe.yaml
# (Slashes in names break expression eval; see manifest_223p.yaml for slash-style column_map only.)
name: demo_high_sat_223p
description: Workshop — supply air over threshold (223P-style scoped slug)
type: expression
flag: demo_high_sat_flag

inputs:
  ahu1_supply_air_temp:
    brick: ahu1_supply_air_temp

params:
  hi: 100.0

expression: |
  ahu1_supply_air_temp > hi
