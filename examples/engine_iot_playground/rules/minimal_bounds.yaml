# Same schema as Open-FDD platform examples — trimmed for IoT / CSV demos.
name: supply_air_temp_bounds
type: bounds
flag: bad_sensor_flag

params:
  units: imperial

inputs:
  Supply_Air_Temperature_Sensor:
    brick: Supply_Air_Temperature_Sensor
    column: supply_air_temp_f
    bounds:
      imperial: [50, 90]
      metric: [10, 32]
